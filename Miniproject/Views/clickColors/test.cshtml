@model IEnumerable<Miniproject.Models.clickColor>

@{
    ViewBag.Title = "test";
}

<h2>test</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.color)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.colorWord)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.color)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.colorWord)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.clickColorID }) |
            @Html.ActionLink("Details", "Details", new { id=item.clickColorID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.clickColorID })
        </td>
    </tr>
}

</table>
